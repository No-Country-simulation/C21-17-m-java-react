spring:
  application:
    name: msvc-course
  config:
    import: optional:configserver:http://localhost:8888

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled:  true
    path: /docs/swagger-ui.Course