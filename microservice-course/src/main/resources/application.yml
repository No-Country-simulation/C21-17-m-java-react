
spring:
  application:
    name: msvc-course
  config:
    import: optional:configserver:http://ec2-52-91-223-158.compute-1.amazonaws.com:8888

  sql:
    init:
      mode: always

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled:  true
    path: /docs/swagger-ui.Course