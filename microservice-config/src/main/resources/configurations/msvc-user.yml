eureka:
  instance:
    hostname: ec2-3-86-80-212.compute-1.amazonaws.com
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

server:
  port: 9030

spring:
  application:
    name: msvc-user
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/elearning_users_db
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update  # O 'create' seg�n tu caso
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect






